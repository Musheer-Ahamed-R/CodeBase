-> Cascading Style Sheets(CSS) is a tyle sheet language used for describing the look and formatting of a document written in a markup language.

-> Can completely control how our web page look without changing our HTML

-> Each one of the style is called a property.(e.g background-color is a property)